#include <bits/stdc++.h>
using namespace std;

void patternmatching(string X, string Y)
{
    int p = 5; //从小于M的素数集中随机选择p
    int j = 0;
    int n = X.size(), m = Y.size();
    int W = pow(2, m) % p;

}

int main()
{
    //暴力比较mn复杂度，蒙特卡洛算法m+n复杂度，拉斯维加斯算法一样

    return 0;
}
