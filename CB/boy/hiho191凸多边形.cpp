#include <bits/stdc++.h>
using namespace std;

int main()
{
    int N, M, A[105], B[105];
    cin >> N >> M;
    for(int i = 0; i < N; i++) cin >> A[i] >> B[i];
    for(int i = 0; i < N; i++) {
        for(int j = )
    }
    return 0;
}
