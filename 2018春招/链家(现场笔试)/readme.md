# 1、有无限的1、2、5角钱，输入金额求多少种方案？
# 2、两个大数乘法。
# 3、求两个长整型整数二进制中对应位不同的个数。
# 4、一个字符串通过插入、删除、替换等最少操作转换成另一个字符串。
# 5、一个二维矩阵，按照螺旋顺序输出蛇阵。
# 6、{['A','B'],['A','C'],...}，B是A的子目录或子文件，输出树形结构目录。
# 7、给定一个链表，删除里面重复的数字并留下一个。
# 8、删除数组中出现次数大于2的数字并输出。
# 9、一个数组，所有奇数放在偶数前面。
# 10、一个字符串删除多少字符使之变成最长回文串。