#include <bits/stdc++.h>
using namespace std;

int main()
{
	set<int> s, q;
	for (int i = 0; i < 2; i++) {
		s.clear();
		cout << q.size() << ' ';
		s.insert(1);
		s.insert(2);
		for (int j : s) q.insert(j);
		cout << q.size() << endl;
	}
	return 0;
}


虚拟专用网络
同义词 VPN一般指虚拟专用网络
本词条由“科普中国”科学百科词条编写与应用工作项目 审核 。
虚拟专用网络(VPN)的功能是：在公用网络上建立专用网络，进行加密通讯。在企业网络中有广泛应用。VPN网关通过对数据包的加密和数据包目标地址的转换实现远程访问。VPN可通过服务器、硬件、软件等多种方式实现。
专用网络 编辑
在互联网的地址架构中，专用网络是指遵守RFC 1918和RFC 4193规范，使用私有IP地址空间的网络。私有IP无法直接连接互联网，需要公网IP转发。与公网IP相比，私有IP是免费的，也节省了IP地址资源，适合在局域网使用。私有IP地址在internet中不会被分配。


[小黄条](http://www.6fcsj.com/)
【小男孩桌面便签】{http://www.notesmaker.com/call.html}