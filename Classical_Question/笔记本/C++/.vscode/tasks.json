{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build", //这个地方也是build，跟上面保持高度一致性
            "type": "shell",
            "command": "g++",
            "args": [
                "-g","${workspaceRoot}/${fileBasenameNoExtension}.cpp",

                "-o","a.out"
            ]
        }
    ]
}